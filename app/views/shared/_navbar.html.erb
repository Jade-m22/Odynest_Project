<header>
  <nav class="navbar navbar-expand-lg">
    <section class="container">
      <!-- ✅ Logo / lien vers la page d’accueil -->
      <%= link_to "Odynest", root_path, class: "navbar-brand playfair" %>

      <!-- ✅ Bouton hamburger pour mobile -->
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <!-- ✅ Navigation principale -->
      <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
        <ul class="navbar-nav align-items-center">
          <% if current_provider %>
            <!-- ✅ Liens pour prestataires -->
            ...
          <% else %>
            <!-- ✅ Liens pour utilisateurs ou visiteurs -->
            ...
          <% end %>

          <% if provider_signed_in? %>
            <!-- ✅ Menu dropdown pour prestataire connecté -->
            ...
          <% elsif user_signed_in? %>
            <!-- ✅ Menu dropdown pour utilisateur connecté (ou admin) -->
            ...
          <% else %>
            <!-- ✅ Boutons de connexion/inscription -->
            ...
          <% end %>
        </ul>
      </div>
    </section>
  </nav>
</header>
