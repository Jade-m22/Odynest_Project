<!DOCTYPE html>
<html lang="fr">
  <head>
    <title><%= content_for(:title) || "Odynest Project" %></title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Balise Canonique -->
    <link rel="canonical" href="https://odynest.com/">

    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,100..900;1,100..900&family=Playfair+Display+SC:ital,wght@0,400;0,700;0,900;1,400;1,700;1,900&display=swap" rel="stylesheet">

    <!-- Favicon -->
    <link rel="icon" href="<%= asset_path('logo.png') %>" type="image/png">

    <!-- FontAwesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">

    <!-- OpenDys -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/antijingoist/opendyslexic/web/opendyslexic.css">

    <!-- Bootstrap -->
    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css", media: "all" %>
    <%= javascript_include_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js", defer: true %>

    <!-- Application Styles & Scripts -->
    <%= stylesheet_link_tag 'application', media: 'all', "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="<%= [
    ('opendys-font' if session[:dyslexic_font]),
    (session[:daltonism_mode] unless session[:daltonism_mode] == 'normal')
  ].compact.join(' ') %>">

    <header>
      <%= render 'shared/navbar' %>  <!-- Inclusion de la navbar -->
    </header>

    <main>
      <%= yield %>
    </main>

    <%= javascript_include_tag "chatbot", defer: true %>

    <footer>
      <%= render 'shared/footer' %>  <!-- Inclusion du footer -->
    </footer>
  </body>
</html>
